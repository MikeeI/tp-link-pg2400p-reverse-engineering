# Filesystem and web asset inventory
sourceTar=data/extracted-1.0.3-build-20221213-v1/raw/FFS.tar.xz
sourceTarSha256=6f09ac6c0c876d6cdf54658bb08a1870ee3765e1ce7e34c53403575e2842de18
[confirmed] POSIX TAR preflight: 181 members; 125 regular files; 56 directories; 0 symlinks, hardlinks, devices, absolute paths, or `..` paths. Extracted with GNU tar preserving no source ownership or permissions.
[confirmed] FFS archive contains `logfile/logfile.cfg` and compressed `web/` assets. 124 individual XZ files decompressed successfully with liblzma; 0 failures; compressed asset bytes=604168; decoded bytes=1856113.
[confirmed] Decoded `web/` tree has 124 files. Major static paths: `config/`, `locale/`, `js/app/`, `js/su/`, `js/libs/`, `models/`, `modules/`, and `themes/default/`.
[confirmed] UI navigation is data-driven: `config/navigator.json` exposes Status/networkMap; PLC (settings, soft pair, power saving, compatibility); Network (LAN, QoS); System (LED, time, accessibility if present, firmware, factory reset, reboot, change password).
[likely] This FFS TAR is the web filesystem deployed by the runtime. The descriptor explicitly names `/web` and `FFS.tar.xz.upg`, but mounting/install behavior needs updater/runtime evidence.

`web-asset-inventory.tsv` is a deterministic path, decoded-byte-size, SHA-256 inventory. All web source offsets in sibling evidence are decoded-file byte offsets bound to its listed hash.
